# Saudação (block_greeting) — molde

Ponto de partida do **exercício 1** do Moodle Plugin Lab: um bloco que exibe uma frase.

- `SCOPE.md` descreve o que construir.
- O esqueleto (`version.php`, `block_greeting.php`, `lang/`) já está aqui; falta a lógica.
- `tests/greeting_test.php` **falha** de propósito até você fazer o bloco renderizar a
  string `greeting` — é o seu alvo.
- O CI (`.github/workflows/ci.yml`) roda um job por critério.

Passo a passo: veja `docs/exemplo/PASSOS.md` no repositório do laboratório.

## Licença

GPL v3 ou posterior.
